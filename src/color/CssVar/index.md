---
nav: Colors
title: CssVar
description: The CssVar component is a memoized component used to display a color scale. It receives props containing the name of the scale, the color scale items, and a number representing the index of the mid-highlight. The component renders a set of ScaleRow components, each representing a different part of the color scale.
---

## Primary

```tsx
import { CssVar, primary } from '@lobehub/ui/color';

export default () => <CssVar name="primary" scale={primary} midHighLight={9} />;
```

## Colors

### Red

```tsx
import { CssVar, red } from '@lobehub/ui/color';

export default () => <CssVar name="red" scale={red} midHighLight={9} />;
```

### Volcano

```tsx
import { CssVar, volcano } from '@lobehub/ui/color';

export default () => <CssVar name="volcano" scale={volcano} midHighLight={9} />;
```

### Orange

```tsx
import { CssVar, orange } from '@lobehub/ui/color';

export default () => <CssVar name="orange" scale={orange} midHighLight={9} />;
```

### Gold

```tsx
import { CssVar, gold } from '@lobehub/ui/color';

export default () => <CssVar name="gold" scale={gold} midHighLight={9} />;
```

### Yellow

```tsx
import { CssVar, yellow } from '@lobehub/ui/color';

export default () => <CssVar name="yellow" scale={yellow} midHighLight={9} />;
```

### Lime

```tsx
import { CssVar, lime } from '@lobehub/ui/color';

export default () => <CssVar name="lime" scale={lime} midHighLight={9} />;
```

### Green

```tsx
import { CssVar, green } from '@lobehub/ui/color';

export default () => <CssVar name="green" scale={green} midHighLight={9} />;
```

### Cyan

```tsx
import { CssVar, cyan } from '@lobehub/ui/color';

export default () => <CssVar name="cyan" scale={cyan} midHighLight={9} />;
```

### Blue

```tsx
import { CssVar, blue } from '@lobehub/ui/color';

export default () => <CssVar name="blue" scale={blue} midHighLight={9} />;
```

### Geekblue

```tsx
import { CssVar, geekblue } from '@lobehub/ui/color';

export default () => <CssVar name="geekblue" scale={geekblue} midHighLight={9} />;
```

### Purple

```tsx
import { CssVar, purple } from '@lobehub/ui/color';

export default () => <CssVar name="purple" scale={purple} midHighLight={9} />;
```

### Magenta

```tsx
import { CssVar, magenta } from '@lobehub/ui/color';

export default () => <CssVar name="magenta" scale={magenta} midHighLight={9} />;
```

## Neutral

### Gray

```tsx
import { CssVar, gray } from '@lobehub/ui/color';

export default () => <CssVar name="gray" scale={gray} midHighLight={9} />;
```

## Extra Neutral

### Mauve

```tsx
import { CssVar, mauve } from '@lobehub/ui/color';

export default () => <CssVar name="mauve" scale={mauve} midHighLight={9} />;
```

### Olive

```tsx
import { CssVar, olive } from '@lobehub/ui/color';

export default () => <CssVar name="olive" scale={olive} midHighLight={9} />;
```

### Sage

```tsx
import { CssVar, sage } from '@lobehub/ui/color';

export default () => <CssVar name="sage" scale={sage} midHighLight={9} />;
```

### Sand

```tsx
import { CssVar, sand } from '@lobehub/ui/color';

export default () => <CssVar name="sand" scale={sand} midHighLight={9} />;
```

### Slate

```tsx
import { CssVar, slate } from '@lobehub/ui/color';

export default () => <CssVar name="slate" scale={slate} midHighLight={9} />;
```
