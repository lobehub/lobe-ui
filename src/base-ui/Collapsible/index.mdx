---
title: Collapsible
description: Controlled show / hide with the same height and fade motion as Accordion panels, for sections that render their own header.
category: Data Display
---

import DemoIndex from './demos/index.tsx?demo';

## Default

`open` drives the panel; the content is unmounted when closed unless `keepMounted` is set.

<Demo of={DemoIndex} layout="bare" />

## APIs

<Api name="Collapsible" from=".." />
