import { FluidGradient } from '@lobehub/ui/awesome';

export default () => (
  <div style={{ blockSize: 320, overflow: 'hidden', position: 'relative' }}>
    <FluidGradient intensity={0.6} />
  </div>
);
