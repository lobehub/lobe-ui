import { Empty } from '@lobehub/ui';

export default () => (
  <Empty
    description="Drop a .skill file or create one from a template."
    title="No skills yet"
    variant="dashed"
    onClick={() => {}}
  />
);
