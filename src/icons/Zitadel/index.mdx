---
title: Zitadel
description: Provides the Zitadel monochrome icon and avatar variant, together with title and primary-color metadata.
category: Auth Icons
---

## Icons

```tsx
import { Zitadel } from '@lobehub/ui/icons';

import { Flexbox } from '@/Flex';

export default () => <Zitadel size={64} />;
```

## Color

```tsx
import { Zitadel } from '@lobehub/ui/icons';

import { Flexbox } from '@/Flex';

export default () => <Zitadel.Color size={64} />;
```

## Avatars

```tsx
import { Zitadel } from '@lobehub/ui/icons';

import { Flexbox } from '@/Flex';

export default () => (
  <Flexbox gap={16} horizontal>
    <Zitadel.Avatar size={64} />
    <Zitadel.Avatar size={64} shape={'square'} />
  </Flexbox>
);
```
